AUTH_SECRET=
CREDENTIALS_ENCRYPTION_KEY=
SIGNUP_INVITE_CODE=
# Optional. Akahu hands these out on request (hello@akahu.nz); without one the
# transactions page shows only the categories Akahu sends with each transaction.
GENIE_API_TOKEN=
# Set to 1 to put the invented demo people on this deployment. Off unless
# it says 1, and a deployment with it on can never reach a real bank.
BUDGE_DEMO=
