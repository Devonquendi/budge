AUTH_SECRET=
CREDENTIALS_ENCRYPTION_KEY=
SIGNUP_INVITE_CODE=
# Optional. Akahu hands these out on request (hello@akahu.nz); without one the
# transactions page shows only the categories Akahu sends with each transaction.
GENIE_API_TOKEN=
